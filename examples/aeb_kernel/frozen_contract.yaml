# Component F (Tier 3, structural separation): the FROZEN CONTRACT for
# aeb_kernel.dfy - the canonical, AST-normalized contract surface: every
# datatype/codatatype definition, and per function/method its signature +
# requires + ensures, plus function/predicate bodies (which ARE the spec).
# GENERATED by evidence/frozen_contract.py::build_frozen_contract from the
# human-authored spec, then committed and drift-checked - do not hand-edit.
# check_contract proves any candidate .dfy preserves this surface exactly and
# introduces no forbidden soundness escape (assume / {:axiom} / {:extern}).
# method bodies are NOT frozen (implementation, where proof scaffolding lives).
spec: aeb_kernel.dfy
forbidden_constructs:
- assume
- '{:axiom}'
- '{:extern}'
declarations:
- kind: datatype
  name: TargetType
  definition: datatype TargetType = LeadVehicleTarget | PedestrianTarget
- kind: function
  name: FCWRequiredActive
  signature: 'function FCWRequiredActive ( target : TargetType , speedKmh : real ) : bool'
  requires: []
  ensures:
  - target == LeadVehicleTarget ==> ( FCWRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh <
    145.0 ) )
  - target == PedestrianTarget ==> ( FCWRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh <
    73.0 ) )
  body: '{ match target case LeadVehicleTarget = > 10.0 < speedKmh < 145.0 case PedestrianTarget = > 10.0
    < speedKmh < 73.0 }'
- kind: function
  name: AEBRequiredActive
  signature: 'function AEBRequiredActive ( target : TargetType , speedKmh : real ) : bool'
  requires: []
  ensures:
  - target == LeadVehicleTarget ==> ( AEBRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh <
    145.0 ) )
  - target == PedestrianTarget ==> ( AEBRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh <
    73.0 ) )
  body: '{ match target case LeadVehicleTarget = > 10.0 < speedKmh < 145.0 case PedestrianTarget = > 10.0
    < speedKmh < 73.0 }'
- kind: function
  name: IsSubjectVehicleBrakingOnset
  signature: 'function IsSubjectVehicleBrakingOnset ( decelG : real ) : bool'
  requires: []
  ensures:
  - IsSubjectVehicleBrakingOnset ( decelG ) <==> decelG >= 0.15
  body: '{ decelG >= 0.15 }'
- kind: function
  name: IsLeadVehicleBrakingOnset
  signature: 'function IsLeadVehicleBrakingOnset ( decelG : real ) : bool'
  requires: []
  ensures:
  - IsLeadVehicleBrakingOnset ( decelG ) <==> decelG >= 0.05
  body: '{ decelG >= 0.05 }'
- kind: function
  name: IsBrakePedalApplicationOnset
  signature: 'function IsBrakePedalApplicationOnset ( forceN : real ) : bool'
  requires: []
  ensures:
  - IsBrakePedalApplicationOnset ( forceN ) <==> forceN >= 11.0
  body: '{ forceN >= 11.0 }'
- kind: function
  name: IsFalseActivationCompliant
  signature: 'function IsFalseActivationCompliant ( peakAdditionalDecelG : real ) : bool'
  requires:
  - peakAdditionalDecelG >= 0.0
  ensures:
  - IsFalseActivationCompliant ( peakAdditionalDecelG ) <==> peakAdditionalDecelG < 0.25
  body: '{ peakAdditionalDecelG < 0.25 }'
