# Contract ratification — `aeb_kernel.dfy`

Generated by `evidence/contract_attestation.py`. This is the human half of Component F (Tier 3, authoring migration): the reviewer works ELIMINATIVELY through every frozen declaration — producing candidate defeaters and eliminating them — and formally adopts the contract. Completing this earns the label **human-ratified** — not "human-authored"; see the module docstring for exactly what this act can and cannot claim.

- Frozen contract: `frozen_contract.yaml`
- Contract hash (sha256): `3e7cf7110ed9bc22fb551bcf6b9bcce54572d12fe5f4ae2e0ee5cf23bbd273c6`
- Blind constant review (folded in): `source_anchored_review_aeb.md` — this ratification is not complete until that review is itself complete (no `_PENDING_` left in it).
- Template: v2 (defeater-based ratification; see module docstring)

## Sign-off

- Reviewer: _PENDING_
- Date: _PENDING_
- Attestation (drafter != checker): _PENDING_ — "I am a human reviewer, and not the system that drafted this spec (drafter != checker)."
- Adoption: _PENDING_ — "Having examined every frozen declaration below against the primary sources, I adopt this contract as my own specification of the requirement (human-ratified)."
- Status: _PENDING_ (a freshly generated artifact is PENDING — structurally valid, not yet ratified)

## How to complete this ratification

First complete `source_anchored_review_aeb.md` (the blind, source-anchored constant review — this is where doubts about the *constants and sources themselves* are raised and eliminated; do not repeat them below). Then, for each declaration below: read the mapped requirement and the frozen contract surface side by side, and work eliminatively — confidence is earned by raising doubts and eliminating them, not by assent. Record in **Wrong-if** the most credible concrete condition under which a clause here would assert the wrong thing (boundary direction, inequality strictness, unit, operand order), and how you eliminated it — or, if you could not eliminate it, stop: the contract needs changing, not ratifying. Record in **Gap-if** the most credible way every clause here could hold while the requirement is still violated (missing case, bound too weak, vacuous guard), and how you eliminated it. Declarations flagged **definitional proof** deserve your strongest doubt: the proof certifies nothing beyond the definition itself, so independent meaning rests entirely on this ratification. Only then record **Adopted?**. Fill the sign-off above and replace every `_PENDING_`. If the spec's contract ever changes, the hash above goes stale and this ratification must be redone against the new contract — the structure gate enforces that mechanically.

## Frozen declarations

### datatype `TargetType`

**Requirement (auto-assembled):** none mapped in `traceability_matrix.a.json` (type declaration or helper/internal — confirm this is expected as part of Gap-if).

```
datatype TargetType = LeadVehicleTarget | PedestrianTarget
```

**Wrong-if (rebutting)?** _PENDING_ (state the concrete condition under which a clause above would assert the wrong thing relative to the requirement, and how it was eliminated — or sustained, which blocks adoption)

**Gap-if (undercutting)?** _PENDING_ (state how every clause above could hold while the requirement is still violated, and how it was eliminated — or sustained, which blocks adoption)

**Adopted?** _PENDING_ (yes / no + notes — recorded only after Wrong-if and Gap-if are eliminated; does this declaration mean what the requirement means?)

### function `FCWRequiredActive`

**Requirement (auto-assembled from `traceability_matrix.a.json`):**
> **REQ-AEB-1** — Forward collision warning vs. a lead vehicle must operate at any forward speed greater than 10 km/h (6.2 mph) and less than 145 km/h (90.1 mph) (§ 571.127 S5.1.1).
> **REQ-AEB-3** — Forward collision warning vs. a pedestrian must operate at any forward speed greater than 10 km/h (6.2 mph) and less than 73 km/h (45.3 mph) (§ 571.127 S5.2.1).

> ⚠ **Definitional proof** — the recorded Dafny evidence for this declaration proves a postcondition that restates the definition (tautology by reflexivity). The proof certifies totality/type-safety/exhaustiveness only; independent meaning rests entirely on this ratification. Weight your Wrong-if / Gap-if accordingly.

```
function FCWRequiredActive ( target : TargetType , speedKmh : real ) : bool
```

- ensures `target == LeadVehicleTarget ==> ( FCWRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh < 145.0 ) )`
- ensures `target == PedestrianTarget ==> ( FCWRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh < 73.0 ) )`
- body (spec) `{ match target case LeadVehicleTarget = > 10.0 < speedKmh < 145.0 case PedestrianTarget = > 10.0 < speedKmh < 73.0 }`

**Sourced literals in this declaration (auto-assembled from `literal_citations.yaml`):**
> `10.0` — nhtsa-fmvss-127-2024.md: "greater than 10 km/h (6.2 mph)"
> `145.0` — nhtsa-fmvss-127-2024.md: "less than 145 km/h (90.1 mph)"
> `73.0` — nhtsa-fmvss-127-2024.md: "less than 73 km/h (45.3 mph)"

**Wrong-if (rebutting)?** _PENDING_ (state the concrete condition under which a clause above would assert the wrong thing relative to the requirement, and how it was eliminated — or sustained, which blocks adoption)

**Gap-if (undercutting)?** _PENDING_ (state how every clause above could hold while the requirement is still violated, and how it was eliminated — or sustained, which blocks adoption)

**Adopted?** _PENDING_ (yes / no + notes — recorded only after Wrong-if and Gap-if are eliminated; does this declaration mean what the requirement means?)

### function `AEBRequiredActive`

**Requirement (auto-assembled from `traceability_matrix.a.json`):**
> **REQ-AEB-2** — Automatic emergency braking vs. a lead vehicle must operate at any forward speed greater than 10 km/h (6.2 mph) and less than 145 km/h (90.1 mph) (§ 571.127 S5.1.2) - a distinct obligation from REQ-AEB-1 (the AEB action, not the FCW warning), confirmed against the source text even though the numeric envelope coincides.
> **REQ-AEB-4** — Automatic emergency braking vs. a pedestrian must operate at any forward speed greater than 10 km/h (6.2 mph) and less than 73 km/h (45.3 mph) (§ 571.127 S5.2.2).

> ⚠ **Definitional proof** — the recorded Dafny evidence for this declaration proves a postcondition that restates the definition (tautology by reflexivity). The proof certifies totality/type-safety/exhaustiveness only; independent meaning rests entirely on this ratification. Weight your Wrong-if / Gap-if accordingly.

```
function AEBRequiredActive ( target : TargetType , speedKmh : real ) : bool
```

- ensures `target == LeadVehicleTarget ==> ( AEBRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh < 145.0 ) )`
- ensures `target == PedestrianTarget ==> ( AEBRequiredActive ( target , speedKmh ) <==> ( 10.0 < speedKmh < 73.0 ) )`
- body (spec) `{ match target case LeadVehicleTarget = > 10.0 < speedKmh < 145.0 case PedestrianTarget = > 10.0 < speedKmh < 73.0 }`

**Sourced literals in this declaration (auto-assembled from `literal_citations.yaml`):**
> `10.0` — nhtsa-fmvss-127-2024.md: "greater than 10 km/h (6.2 mph)"
> `145.0` — nhtsa-fmvss-127-2024.md: "less than 145 km/h (90.1 mph)"
> `73.0` — nhtsa-fmvss-127-2024.md: "less than 73 km/h (45.3 mph)"

**Wrong-if (rebutting)?** _PENDING_ (state the concrete condition under which a clause above would assert the wrong thing relative to the requirement, and how it was eliminated — or sustained, which blocks adoption)

**Gap-if (undercutting)?** _PENDING_ (state how every clause above could hold while the requirement is still violated, and how it was eliminated — or sustained, which blocks adoption)

**Adopted?** _PENDING_ (yes / no + notes — recorded only after Wrong-if and Gap-if are eliminated; does this declaration mean what the requirement means?)

### function `IsSubjectVehicleBrakingOnset`

**Requirement (auto-assembled from `traceability_matrix.a.json`):**
> **REQ-AEB-5** — Subject vehicle braking onset is the point at which the subject vehicle achieves a deceleration of 0.15 g due to automatic control of the service brakes (§ 571.127 S4).

> ⚠ **Definitional proof** — the recorded Dafny evidence for this declaration proves a postcondition that restates the definition (tautology by reflexivity). The proof certifies totality/type-safety/exhaustiveness only; independent meaning rests entirely on this ratification. Weight your Wrong-if / Gap-if accordingly.

```
function IsSubjectVehicleBrakingOnset ( decelG : real ) : bool
```

- ensures `IsSubjectVehicleBrakingOnset ( decelG ) <==> decelG >= 0.15`
- body (spec) `{ decelG >= 0.15 }`

**Sourced literals in this declaration (auto-assembled from `literal_citations.yaml`):**
> `0.15` — nhtsa-fmvss-127-2024.md: "deceleration of 0.15 g due to the automatic control of the service brakes"

**Wrong-if (rebutting)?** _PENDING_ (state the concrete condition under which a clause above would assert the wrong thing relative to the requirement, and how it was eliminated — or sustained, which blocks adoption)

**Gap-if (undercutting)?** _PENDING_ (state how every clause above could hold while the requirement is still violated, and how it was eliminated — or sustained, which blocks adoption)

**Adopted?** _PENDING_ (yes / no + notes — recorded only after Wrong-if and Gap-if are eliminated; does this declaration mean what the requirement means?)

### function `IsLeadVehicleBrakingOnset`

**Requirement (auto-assembled from `traceability_matrix.a.json`):**
> **REQ-AEB-6** — Lead vehicle braking onset is the point at which the lead vehicle achieves a deceleration of 0.05 g due to brake application (§ 571.127 S4).

> ⚠ **Definitional proof** — the recorded Dafny evidence for this declaration proves a postcondition that restates the definition (tautology by reflexivity). The proof certifies totality/type-safety/exhaustiveness only; independent meaning rests entirely on this ratification. Weight your Wrong-if / Gap-if accordingly.

```
function IsLeadVehicleBrakingOnset ( decelG : real ) : bool
```

- ensures `IsLeadVehicleBrakingOnset ( decelG ) <==> decelG >= 0.05`
- body (spec) `{ decelG >= 0.05 }`

**Sourced literals in this declaration (auto-assembled from `literal_citations.yaml`):**
> `0.05` — nhtsa-fmvss-127-2024.md: "0.05 g due to brake application"

**Wrong-if (rebutting)?** _PENDING_ (state the concrete condition under which a clause above would assert the wrong thing relative to the requirement, and how it was eliminated — or sustained, which blocks adoption)

**Gap-if (undercutting)?** _PENDING_ (state how every clause above could hold while the requirement is still violated, and how it was eliminated — or sustained, which blocks adoption)

**Adopted?** _PENDING_ (yes / no + notes — recorded only after Wrong-if and Gap-if are eliminated; does this declaration mean what the requirement means?)

### function `IsBrakePedalApplicationOnset`

**Requirement (auto-assembled from `traceability_matrix.a.json`):**
> **REQ-AEB-7** — Brake pedal application onset is when 11 N of force has been applied to the brake pedal (§ 571.127 S4).

> ⚠ **Definitional proof** — the recorded Dafny evidence for this declaration proves a postcondition that restates the definition (tautology by reflexivity). The proof certifies totality/type-safety/exhaustiveness only; independent meaning rests entirely on this ratification. Weight your Wrong-if / Gap-if accordingly.

```
function IsBrakePedalApplicationOnset ( forceN : real ) : bool
```

- ensures `IsBrakePedalApplicationOnset ( forceN ) <==> forceN >= 11.0`
- body (spec) `{ forceN >= 11.0 }`

**Sourced literals in this declaration (auto-assembled from `literal_citations.yaml`):**
> `11.0` — nhtsa-fmvss-127-2024.md: "11 N of force has been applied to the brake pedal"

**Wrong-if (rebutting)?** _PENDING_ (state the concrete condition under which a clause above would assert the wrong thing relative to the requirement, and how it was eliminated — or sustained, which blocks adoption)

**Gap-if (undercutting)?** _PENDING_ (state how every clause above could hold while the requirement is still violated, and how it was eliminated — or sustained, which blocks adoption)

**Adopted?** _PENDING_ (yes / no + notes — recorded only after Wrong-if and Gap-if are eliminated; does this declaration mean what the requirement means?)

### function `IsFalseActivationCompliant`

**Requirement (auto-assembled from `traceability_matrix.a.json`):**
> **REQ-AEB-8** — False activation: the vehicle must not automatically apply braking that results in peak additional deceleration exceeding what manual braking would produce by 0.25 g or greater (§ 571.127 S5.3).

> ⚠ **Definitional proof** — the recorded Dafny evidence for this declaration proves a postcondition that restates the definition (tautology by reflexivity). The proof certifies totality/type-safety/exhaustiveness only; independent meaning rests entirely on this ratification. Weight your Wrong-if / Gap-if accordingly.

```
function IsFalseActivationCompliant ( peakAdditionalDecelG : real ) : bool
```

- requires `peakAdditionalDecelG >= 0.0`
- ensures `IsFalseActivationCompliant ( peakAdditionalDecelG ) <==> peakAdditionalDecelG < 0.25`
- body (spec) `{ peakAdditionalDecelG < 0.25 }`

**Sourced literals in this declaration (auto-assembled from `literal_citations.yaml`):**
> `0.25` — nhtsa-fmvss-127-2024.md: "exceeds what manual braking would produce by 0.25 g"

**Wrong-if (rebutting)?** _PENDING_ (state the concrete condition under which a clause above would assert the wrong thing relative to the requirement, and how it was eliminated — or sustained, which blocks adoption)

**Gap-if (undercutting)?** _PENDING_ (state how every clause above could hold while the requirement is still violated, and how it was eliminated — or sustained, which blocks adoption)

**Adopted?** _PENDING_ (yes / no + notes — recorded only after Wrong-if and Gap-if are eliminated; does this declaration mean what the requirement means?)
