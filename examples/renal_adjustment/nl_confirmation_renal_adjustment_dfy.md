# Gate C6 sign-off: `renal_adjustment.dfy`

## Purpose

Gate C6 (NL-dialogue confirmation) exists to catch a spec/requirement-text
mismatch at authoring time rather than at review time. Per the
infrastructure plan (`/root/.claude/plans/stateless-weaving-firefly.md`),
this gate is applied earlier in this POC's build order than it was for
`dosage.dfy` — immediately after the spec first verifies clean, not
deferred behind STPs/mutation testing.

## A real generalization gap found and fixed before this summary could be generated

The infrastructure plan assumed `evidence/dafny_nl_summary.py::summarize_method`
"generalizes for free" since it's parameterized by `method_name`. That
was checked, not trusted — and it was wrong in one respect:
`_find_method_header` (the shared helper this module, `dafny_spec_lint.py`,
and `dafny_mutate.py` all depend on) only matched Dafny's `method`
keyword, never `function`. Every real Dafny declaration this repo had
summarized before today was a `method` (`dosage.dfy`'s `ExpectedDose`
function was never passed to this extractor directly) — `renal_adjustment.dfy`
is pure functions, five of them, zero methods, and the gap only surfaced
because this is the first time the tooling was actually pointed at one.

Fixed by widening the shared regex to match `method` or `function`
(`evidence/dafny_spec_lint.py::_find_method_header`, and the duplicate
lookup in `evidence/dafny_mutate.py::_method_header_span` for
consistency) — a small, bounded extension, not a redesign, confirmed by
running the real file through it rather than assumed from the code
reading generic. Two new regression tests
(`tests/test_dafny_nl_summary.py::test_summarizes_a_function_not_just_a_method`,
`tests/test_dafny_spec_lint.py::test_requires_clause_extraction_matches_a_function_not_just_a_method`).

**A second, real bug found in the same pass, not a hypothetical:**
`_REQ_ID_RE`'s character class (`[A-Z0-9-]`) excluded lowercase letters,
so it silently truncated `REQ-RENAL-1a` to `REQ-RENAL-1` — citing the
wrong requirement ID rather than dropping the citation visibly.
`dosage.dfy`'s REQ-IDs (`REQ-GIP-1-4-12`, `REQ-GIP-1-8-1`) never had a
lowercase suffix, so this never fired until a second spec actually
exercised it. Fixed (`[A-Za-z0-9-]`), regression-tested
(`test_lowercase_suffixed_req_id_is_cited_in_full_not_truncated`). Also
added missing `// REQ-RENAL-*` trailing citations to every `ensures`
clause in `renal_adjustment.dfy` itself — without them, every
postcondition below would have read "no requirement cited," understating
real, existing traceability rather than reflecting an actual gap.

142 tests passing after both fixes (up from 138); `renal_adjustment.dfy`
re-verified clean after the citation comments were added (`5 verified,
0 errors` — comments don't affect verification).

## Summary presented (generated by `evidence/dafny_nl_summary.py`, 2026-07-08)

### `RoundHalfUp`

```
# Plain-English summary: `RoundHalfUp`

## Parameters
- `x`: real

## Preconditions (must hold before the method runs)
1. `x >= 0.0` — x is at least 0.0

## Postconditions (guaranteed to hold on return)
1. `(RoundHalfUp(x) as real) - 0.5 <= x < (RoundHalfUp(x) as real) + 0.5` — (RoundHalfUp(x) as real) - 0.5 is at most x is less than (RoundHalfUp(x) as real) + 0.5 **[REQ-RENAL-1a]**
```

### `GStage`

```
# Plain-English summary: `GStage`

## Parameters
- `roundedEgfr`: int

## Preconditions (must hold before the method runs)
1. `roundedEgfr >= 0` — roundedEgfr is at least 0

## Postconditions (guaranteed to hold on return)
1. `roundedEgfr >= 90 ==> GStage(roundedEgfr) == G1` — roundedEgfr is at least 90 implies GStage(roundedEgfr) equals G1 **[REQ-RENAL-1]**
2. `60 <= roundedEgfr <= 89 ==> GStage(roundedEgfr) == G2` — 60 is at most roundedEgfr is at most 89 implies GStage(roundedEgfr) equals G2 **[REQ-RENAL-1]**
3. `45 <= roundedEgfr <= 59 ==> GStage(roundedEgfr) == G3a` — 45 is at most roundedEgfr is at most 59 implies GStage(roundedEgfr) equals G3a **[REQ-RENAL-1]**
4. `30 <= roundedEgfr <= 44 ==> GStage(roundedEgfr) == G3b` — 30 is at most roundedEgfr is at most 44 implies GStage(roundedEgfr) equals G3b **[REQ-RENAL-1]**
5. `15 <= roundedEgfr <= 29 ==> GStage(roundedEgfr) == G4` — 15 is at most roundedEgfr is at most 29 implies GStage(roundedEgfr) equals G4 **[REQ-RENAL-1]**
6. `roundedEgfr < 15 ==> GStage(roundedEgfr) == G5` — roundedEgfr is less than 15 implies GStage(roundedEgfr) equals G5 **[REQ-RENAL-1]**
```

### `SelectFormula`

```
# Plain-English summary: `SelectFormula`

## Parameters
- `isDirectActingOralAnticoagulant`: bool
- `isOnNephrotoxicDrug`: bool
- `ageYears`: int
- `bmi`: real
- `isNarrowTherapeuticIndexDrug`: bool

## Preconditions (must hold before the method runs)
1. `ageYears >= 0` — ageYears is at least 0
2. `bmi > 0.0` — bmi is greater than 0.0

## Postconditions (guaranteed to hold on return)
1. `(isDirectActingOralAnticoagulant || isOnNephrotoxicDrug || ageYears >= 75 || bmi < 18.0 || bmi > 40.0 || isNarrowTherapeuticIndexDrug) ==> SelectFormula(...) == CockcroftGaultFormula` — (isDirectActingOralAnticoagulant or isOnNephrotoxicDrug or ageYears is at least 75 or bmi is less than 18.0 or bmi is greater than 40.0 or isNarrowTherapeuticIndexDrug) implies SelectFormula(...) equals CockcroftGaultFormula **[REQ-RENAL-2]**
2. `!(isDirectActingOralAnticoagulant || isOnNephrotoxicDrug || ageYears >= 75 || bmi < 18.0 || bmi > 40.0 || isNarrowTherapeuticIndexDrug) ==> SelectFormula(...) == EGFRFormula` — !(isDirectActingOralAnticoagulant or isOnNephrotoxicDrug or ageYears is at least 75 or bmi is less than 18.0 or bmi is greater than 40.0 or isNarrowTherapeuticIndexDrug) implies SelectFormula(...) equals EGFRFormula **[REQ-RENAL-2]**
```

*(the repeated argument list inside `SelectFormula(...)` above is
abbreviated for readability in this document only — the actual generated
summary spells out all five arguments in full each time; see the raw
capture if needed.)*

### `ComposedCeiling`

```
# Plain-English summary: `ComposedCeiling`

## Parameters
- `existingCeiling`: real
- `renalCeiling`: real

## Preconditions (must hold before the method runs)
1. `existingCeiling > 0.0` — existingCeiling is greater than 0.0
2. `renalCeiling > 0.0` — renalCeiling is greater than 0.0

## Postconditions (guaranteed to hold on return)
1. `ComposedCeiling(existingCeiling, renalCeiling) <= existingCeiling` — ComposedCeiling(existingCeiling, renalCeiling) is at most existingCeiling **[REQ-RENAL-5]**
2. `ComposedCeiling(existingCeiling, renalCeiling) <= renalCeiling` — ComposedCeiling(existingCeiling, renalCeiling) is at most renalCeiling **[REQ-RENAL-5]**
```

### `AssessRenalFunction`

```
# Plain-English summary: `AssessRenalFunction`

## Parameters
- `formula`: Formula
- `renalFunctionValue`: real

## Preconditions (must hold before the method runs)
1. `renalFunctionValue >= 0.0` — renalFunctionValue is at least 0.0

## Postconditions (guaranteed to hold on return)
1. `formula == EGFRFormula ==> AssessRenalFunction(formula, renalFunctionValue).EGFRAssessment?` — formula equals EGFRFormula implies AssessRenalFunction(formula, renalFunctionValue).EGFRAssessment? **[REQ-RENAL-1, REQ-RENAL-2]**
2. `formula == CockcroftGaultFormula ==> AssessRenalFunction(formula, renalFunctionValue).CrClAssessment?` — formula equals CockcroftGaultFormula implies AssessRenalFunction(formula, renalFunctionValue).CrClAssessment? **[REQ-RENAL-1, REQ-RENAL-2]**
```

**Reading note on `AssessRenalFunction`'s citations:** these two clauses
don't map to a single REQ-ID the way the others do — they're the actual
mechanism enforcing Gate 1c Finding 2's resolution (an eGFR value can
never be mislabeled with a raw CrCl result, or vice versa). Cited to
both REQ-RENAL-1 (correct G-stage integrity) and REQ-RENAL-2 (formula
selection is what drives the branch), since that's what's actually being
protected — named here explicitly rather than left for a reader to infer
from the dual citation alone.

## What this spec does NOT yet cover (named, not implied)

Per `renal_adjustment.dfy`'s own header comment and `PHASE1_PLAN.md`:
REQ-RENAL-3 (Cockcroft-Gault reliability override), REQ-RENAL-4
(missing-data fail-safe), REQ-RENAL-6 (AKI reassessment flag), and
REQ-RENAL-7 (BSA de-normalization) are not yet formalized as Dafny
signatures — prose only so far. REQ-RENAL-8 (classification-flag
provenance) and the CrCl/eGFR computation question (Gate 1c Finding 1)
are both closed under named, provisional fallback assumptions per
`PHASE1_PLAN.md`, not built into this spec.

## Decision

**Pending — presented for Steven's confirmation, not decided here.**
Per this gate's own purpose, the summary above needs an explicit human
check against intent before it counts as signed off, the same standard
`dosage.dfy`'s Gate C6 sign-off was held to.

## Amendment 2026-07-09 — Gate C4 found and fixed two real gaps in `ComposedCeiling` and `AssessRenalFunction`

Gate C4's STP suite (`gate_c4_stp_plan.md`'s hand-derived predictions,
built and run for real — see `DEVLOG.md`) confirmed both predicted
gaps mechanically: `ComposedCeiling`'s original two `<=` bounds and
`AssessRenalFunction`'s original two variant-only clauses did not pin
the exact result — REJECT lemmas assuming a wrong candidate value
**failed to verify** against the original spec (preserved as
`renal_adjustment_underconstrained.dfy`; the genuinely failing capture
is `raw_dafny_output_stp_suite_against_underconstrained_renal.txt`,
`0 verified, 4 errors`). Fixed by adding one pinning clause to
`ComposedCeiling` and two to `AssessRenalFunction`, mirroring
`ExpectedDose`'s role in `dosage.dfy` exactly. Re-verified clean
(`5 verified, 0 errors`, unchanged) and the full STP suite re-run to
confirm the fix: all 44 lemmas (ACCEPT and REJECT) now pass —
`raw_dafny_output_stp_suite_renal.txt`.

Since this touched two functions already signed off above, this is
reported as an amendment rather than a silent edit, per the same
discipline `dosage.dfy`'s own REQ-GIP-1-8-1 tightening amendment used.

### Re-generated summaries (`evidence/dafny_nl_summary.py`, 2026-07-09, post-fix)

```
# Plain-English summary: `ComposedCeiling`

## Parameters
- `existingCeiling`: real
- `renalCeiling`: real

## Preconditions (must hold before the method runs)
1. `existingCeiling > 0.0` — existingCeiling is greater than 0.0
2. `renalCeiling > 0.0` — renalCeiling is greater than 0.0

## Postconditions (guaranteed to hold on return)
1. `ComposedCeiling(existingCeiling, renalCeiling) <= existingCeiling` — ComposedCeiling(existingCeiling, renalCeiling) is at most existingCeiling **[REQ-RENAL-5]**
2. `ComposedCeiling(existingCeiling, renalCeiling) <= renalCeiling` — ComposedCeiling(existingCeiling, renalCeiling) is at most renalCeiling **[REQ-RENAL-5]**
3. `ComposedCeiling(existingCeiling, renalCeiling) == existingCeiling || ComposedCeiling(existingCeiling, renalCeiling) == renalCeiling` — ComposedCeiling(existingCeiling, renalCeiling) equals existingCeiling or ComposedCeiling(existingCeiling, renalCeiling) equals renalCeiling **[REQ-RENAL-5]**
```

```
# Plain-English summary: `AssessRenalFunction`

## Parameters
- `formula`: Formula
- `renalFunctionValue`: real

## Preconditions (must hold before the method runs)
1. `renalFunctionValue >= 0.0` — renalFunctionValue is at least 0.0

## Postconditions (guaranteed to hold on return)
1. `formula == EGFRFormula ==> AssessRenalFunction(formula, renalFunctionValue).EGFRAssessment?` — formula equals EGFRFormula implies AssessRenalFunction(formula, renalFunctionValue).EGFRAssessment? **[REQ-RENAL-1, REQ-RENAL-2]**
2. `formula == CockcroftGaultFormula ==> AssessRenalFunction(formula, renalFunctionValue).CrClAssessment?` — formula equals CockcroftGaultFormula implies AssessRenalFunction(formula, renalFunctionValue).CrClAssessment? **[REQ-RENAL-1, REQ-RENAL-2]**
3. `formula == EGFRFormula ==> AssessRenalFunction(formula, renalFunctionValue) == EGFRAssessment(GStage(RoundHalfUp(renalFunctionValue)))` — formula equals EGFRFormula implies AssessRenalFunction(formula, renalFunctionValue) equals EGFRAssessment(GStage(RoundHalfUp(renalFunctionValue))) **[REQ-RENAL-1, REQ-RENAL-2]**
4. `formula == CockcroftGaultFormula ==> AssessRenalFunction(formula, renalFunctionValue) == CrClAssessment(RoundHalfUp(renalFunctionValue))` — formula equals CockcroftGaultFormula implies AssessRenalFunction(formula, renalFunctionValue) equals CrClAssessment(RoundHalfUp(renalFunctionValue)) **[REQ-RENAL-1, REQ-RENAL-2]**
```

Only these two functions changed (one new clause on `ComposedCeiling`,
two new clauses on `AssessRenalFunction`); `RoundHalfUp`, `GStage`, and
`SelectFormula` are untouched. Presented for confirmation alongside the
original summary above — the whole document, not just this amendment,
is what's pending sign-off.
