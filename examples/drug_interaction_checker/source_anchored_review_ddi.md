# Source-anchored Gate C6 review — `drug_interaction_checker.dfy`

Generated by `evidence/source_anchored_review.py`. This is the source-anchored, blind reform of Gate C6 (Component D): each numeric constant is checked against the verbatim source it transcribes, and the reviewer records what the source implies BEFORE the spec's answer is revealed. See the module docstring for why drafter != checker.

## Sign-off

- Reviewer: _PENDING_
- Date: _PENDING_
- Attestation (drafter != checker): _PENDING_ — "I am a human reviewer, and not the system that drafted this spec (drafter != checker)."
- Status: _PENDING_ (a freshly generated template is PENDING — structurally valid, not yet reviewed)

## How to complete this review

For each constant below: read the **Source says** quote, write in **Blind expectation** the value or boundary you expect the spec to use *from the source alone*, THEN expand **Spec uses** and record in **Captures the source** whether they agree. Fill the sign-off above and replace every `_PENDING_`.

## Source-anchored constants

### 1. `sps-doac-interactions-2024.md`

**Source says (verbatim):**

> Reduce the dose of dabigatran to 110mg twice daily

**Blind expectation** (fill from the source above, before revealing the spec's value): _PENDING_

<details><summary><b>Spec uses</b> (reveal only after filling the blind expectation)</summary>

The spec's constant here is `110`.

</details>

**Captures the source?** _PENDING_ (yes / no + notes)

### 2. `sps-doac-interactions-2024.md`

**Source says (verbatim):**

> Reduce the edoxaban dose to 30mg daily

**Blind expectation** (fill from the source above, before revealing the spec's value): _PENDING_

<details><summary><b>Spec uses</b> (reveal only after filling the blind expectation)</summary>

The spec's constant here is `30`.

</details>

**Captures the source?** _PENDING_ (yes / no + notes)

## Declared constants (no source to blind-check)

- `0` — **structural**: Integer zero: the return of the unreachable wildcard match arm (case _ => (assert false; 0)), proven dead by DoseReductionTargetMg's precondition. A structural default value, not a source-transcribed one.
